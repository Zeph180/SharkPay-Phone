import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ResetPaasswordPage } from './reset-paassword.page';

describe('ResetPaasswordPage', () => {
  let component: ResetPaasswordPage;
  let fixture: ComponentFixture<ResetPaasswordPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ResetPaasswordPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
